{
  "scripts": {
    "vercel-build": "next build && cp ./source.json ./config/${MESSAGE_FILE_NAME:-not-set.json}"
  },
  "dependencies": {
    "next": "^11.0.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
